# Service to expose the ecoligo application
apiVersion: v1
kind: Service
metadata:
  name: ecoligo-app
  labels:
    app: ecoligo-app
spec:
  selector:
    app: ecoligo-app
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
